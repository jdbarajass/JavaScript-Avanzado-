// Con esta practica lo que queremos hacer es validar si una variable un objeto existe como tal, en mi codigo
// entonces lo que hacemos es ponerlo en un condicional el cual nos dira si esta misma existe por medio
// de tru or false
// esto se hace con el fin de saber si puedo o no usar ese mismo nombre para crear otro objeto o variable
var persona = {};

console.log(persona);

if (persona) {
  console.log("La persona existe");
} else {
  console.log("La persona NO existe");
}
var validar;
if (validar) {
  console.log("Si existe");
} else "no existe";
