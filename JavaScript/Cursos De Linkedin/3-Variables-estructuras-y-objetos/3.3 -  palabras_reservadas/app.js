function myCatch(ball) {
  // No usar nombres para variables o funciones en la cuales se escriban palabras reservadas como en este
  //caso que se estaba usando catch la cual se usa para excepciones (try catch)
  console.log(ball);
}

myCatch("beisball");
